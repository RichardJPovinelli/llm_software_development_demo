# Phase 2 — Fine-tuning dependencies
# Install torch with CUDA 12.1 support (adjust cu121 to match your CUDA version):
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# Then install remaining packages:
#   pip install -r requirements-train.txt

transformers>=4.40.0
peft>=0.10.0
bitsandbytes>=0.43.0
trl>=0.8.6
datasets>=2.19.0
accelerate>=0.29.0
tensorboard>=2.14.0
